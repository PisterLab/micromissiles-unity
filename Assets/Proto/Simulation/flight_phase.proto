syntax = "proto3";

package simulation;

// Flight phase enumeration.
enum FlightPhase {
  INITIALIZED = 0;
  READY = 1;
  BOOST = 2;
  MIDCOURSE = 3;
  TERMINAL = 4;
  BALLISTIC = 5;
  TERMINATED = 6;
}
